# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ISSPassTimes' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ISSPassTimes

  target 'ISSPassTimesTests' do
    inherit! :search_paths
    # Pods for testing
    pod 'Alamofire'
    pod 'ObjectMapper'
  end

pod 'Alamofire'
pod 'ObjectMapper'

end
